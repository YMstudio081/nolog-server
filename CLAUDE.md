# CLAUDE.md — nolog-server（WebSocket リレーサーバー）

このファイルは Claude Code（および開発者）がこのリポジトリで作業するためのガイドです。

## リポジトリの役割と3層構成における位置づけ

**nolog-server** は NOLOG 向けの **WebSocket リレー（中継）サーバー**です。クライアントを「ルーム（room）」単位で束ね、あるクライアントから届いたメッセージを同じルーム内の他クライアントへそのまま中継するだけの、極めてミニマルなバックエンドです。NOLOG プロダクトは以下の3リポジトリで構成されます。

| リポジトリ | 役割 | 位置づけ |
|---|---|---|
| **nolog** | モバイルアプリ本体（クライアント） | E2EE の暗号化・復号はすべてクライアントで完結する |
| **nolog-server**（本リポジトリ） | WebSocket リレーサーバー | **暗号文を中継するだけ。鍵を持たず、平文を復号・保存できない** |
| **nolog-legal** | 法務・サポート静的サイト | プライバシーポリシー / 利用規約 / サポートを Web 公開 |

このサーバーは Render 上にデプロイされ、クライアント（nolog）は `wss://nolog-server.onrender.com` に接続します。**受け取るのは暗号文のみで、復号鍵は一切持ちません。** そのため「中継はするが内容は分からない」という NOLOG の設計を成立させる要（かなめ）です。

> 注: このリポジトリが正（source of truth）です。本体 `nolog` リポジトリの `server/` は同一実装のコピーであり、サーバーロジックの変更は必ずこちら側を起点に行ってください。

## 技術スタック

- **言語 / ランタイム**: Node.js（純粋な JavaScript、CommonJS）
- **フレームワーク**: なし。標準の `http` モジュールでヘルスチェック用 HTTP サーバーを立てるのみ（Express 等は不使用）
- **主要ライブラリ**: `ws`（`^8.19.0`）— 唯一の依存。WebSocket サーバー実装に使用
- **DB**: なし。状態はプロセス内メモリの `Map<roomId, Set<ws>>`（`rooms`）のみ（**永続化なし**）
- **デプロイ先**: Render を想定（ポートは `process.env.PORT || 3000`、`GET /` がヘルスチェック）

## 主要ディレクトリ / ファイル

サブディレクトリはなく、ルート直下のみのフラット構成です。

```
server.js            本体（全ロジック。HTTP ヘルスチェック + WebSocket リレー）
package.json         マニフェスト（依存は ws のみ）
package-lock.json    ロックファイル
README.md            1 行の説明
.gitignore           node_modules
```

### 動作ロジック（`server.js`）
- HTTP: `GET /` に `"NOLOG WebSocket Server Running"` を返すだけ（Render のヘルスチェック用）
- WebSocket: 受信 JSON の `type` で分岐
  - `type === "join"`: `data.roomId` のルームへ接続を登録（`rooms: Map<roomId, Set<ws>>`）
  - それ以外: 同じルーム内の**送信元を除く**全クライアント（`readyState === 1`）へ中継
- 切断（`close`）時にルームから当該接続を削除

## 開発・ビルド・テストのコマンド

```bash
npm install          # 依存インストール（ws のみ）

# 起動（package.json の main は index.js だが、実体は server.js）
node server.js       # ローカル起動。PORT 環境変数で待受ポートを指定可（既定 3000）

PORT=3001 node server.js   # ポートを変えて起動する例
```

- **テスト**: 未整備（`npm test` は `exit 1` のプレースホルダ）。動作確認は WebSocket クライアントで `join` → メッセージ送信 → 別クライアントでの受信を手動確認する。
- **ビルド**: なし（トランスパイル不要の素の Node.js）。
- **デプロイ**: Render にプッシュ連携。起動コマンドは `node server.js`、ヘルスチェックは `/`。

> 注: `package.json` の `"main": "index.js"` は実体（`server.js`）と一致していません。起動は `node server.js` を使用してください。

## コーディング規約

- 素の Node.js（CommonJS, `require`）で記述。依存は最小限に保ち、安易にライブラリを増やさない。
- ロジックは `server.js` に集約されている。中継の単純さ（状態はメモリの `Map` のみ）を維持する。
- ログ出力を追加する場合も、**メッセージ内容・接続元 IP・識別子を記録しない**こと（下記の設計方針を参照）。

## このプロジェクト固有の注意点 —「痕跡を残さない」設計方針

このサーバーは「中継はするが痕跡は残さない」ことが存在意義です。以下は**製品の前提条件**であり、破る変更は入れてはいけません。

- **メッセージを保存しない** — 受信した内容を DB・ファイル・外部サービスへ書き出さない。中継後にメモリからも消える。
- **鍵を持たない / 復号しない** — サーバーは暗号文をそのまま転送するだけ。平文へ復号する処理を追加しない。
- **IP を記録しない** — 接続元 IP やクライアント識別子をロギング・永続化しない。
- **永続ストアを導入しない** — `rooms` はメモリ上の一時状態のみ。DB や外部ストレージを足さない。
- **不要な情報収集をしない** — アクセスログ・解析・テレメトリで内容や識別子を残さない。

これらの主張は本体 `nolog` リポジトリの `scripts/compliance_cross_check.ts` が `server/**`（＝このサーバーのコピー）も対象に機械的に検証します。ロジックを変更したら、本体側でコンプライアンスチェックが通ることを確認してください。

## セキュリティ上の既知の前提

- **認証なし**: `roomId` を知っていれば誰でも `join` できる設計。秘匿性は「roomId の推測困難さ」と「内容が E2EE 済みであること」に依存する。roomId の生成・共有はクライアント（nolog）側の責務。
- **CORS / Origin チェックなし**: WebSocket リレーのため未実装。任意オリジンから接続可能。
- これらは中継サーバーとして意図された設計であり、機密性はサーバーではなくクライアント側の E2EE によって担保される。
